# EverySkill 项目目标

## 一句话目标

EverySkill 是一个可配置的 Agent/Skill 编排 Skill：针对用户提出的问题，智能判断是否需要编排，将问题拆分为边界清晰的子任务，选择合适的 Agent 或 Skill，并生成可解释、可验证、受权限和预算约束的工作流拓扑。

它的交付物是“应该如何编排”的方案，而不是一个固定的业务后端或 Agent 调度平台。

## 要解决的问题

面对同一个用户请求，宿主 Agent 通常需要先回答以下问题：

1. 这个请求能否由主 Agent 直接完成，还是需要调用其他 Skill 或多个 Agent？
2. 如果需要拆分，应该拆成哪些互相独立、边界清晰的子任务？
3. 每个子任务应由哪个 Agent 或 Skill 负责，输入和输出分别是什么？
4. 子任务之间是串行依赖、可并行执行，还是需要混合拓扑？
5. 哪些结果需要汇合、复核、投票、人工确认或重新执行？
6. 在权限、风险、耗时、Token 预算和失败条件下，什么时候应该继续、降级或停止？
7. 如何把最终编排方案解释清楚，并保留足够的上下文、证据和验证结果？

EverySkill 要把这些隐含的判断显式化，减少“所有问题都用一个 Agent”或“无依据地盲目并行”的情况。

## 核心方法：基于图的工作流编排

EverySkill 将一个请求建模为任务图，而不是简单的步骤清单：

- 节点表示一个有明确目标、负责人、输入、输出和权限边界的子任务。
- 有向边表示上下文、数据或控制依赖。
- 无环依赖图（DAG）用于表达可执行顺序和并行边界。
- 入度为零的节点是准备或输入节点；多个出边表示 fan-out；多个入边表示 fan-in/汇合。
- 关键路径用于估算最短必要耗时；并行宽度用于估算并发资源和成本压力。
- 汇合点负责统一证据、处理冲突、保留失败分支和生成下一阶段的最小上下文。
- Review Loop 表示有明确验收标准和停止条件的有限反馈边，不能形成无界循环。
- Human Gate 表示权限不足、风险过高或必须由人做决定的边界。

拓扑选择必须有可说明的收益：只有在任务独立性、置信度提升、耗时降低或质量验证方面存在实际好处时，才引入并行、采样或复核。

## 编排决策范围

Skill 至少应支持以下决策原语，并允许使用者在工作流契约中组合它们：

- `DIRECT`：主 Agent 直接处理
- `ROUTE_ONE`：路由到一个明确的 Skill 或 Agent
- `HANDOFF`：将后续流程所有权移交给明确的 Skill，同时保留请求和权限边界
- `SEQUENTIAL`：存在真实依赖的串行阶段
- `PARALLEL_SECTION`：互相独立的子任务并行执行后汇合
- `PARALLEL_SAMPLE`：使用多个独立尝试提升判断置信度
- `ORCHESTRATOR_WORKERS`：由协调器动态产生和管理 Worker
- `REVIEW_LOOP`：按验收标准进行有限复核和修订
- `HUMAN_GATE`：等待人工授权、确认或决策

拓扑是编排结构，不等同于成功、失败或阻塞状态。每次路由还应单独记录权限、风险、预算、置信度、失败策略、停止条件和验证结果。

## 可配置性目标

使用者应主要通过 Markdown 和 JSON 契约完成定制，而不需要修改编排引擎代码。配置至少包括：

- 可见 Skill 及其匹配边界
- 工作流 ID、版本和适用场景
- 任务节点、负责人、依赖和所需输出
- 串行、并行、汇合、复核和人工门控关系
- 上下文传递和最小披露规则
- 权限范围、预算上限、失败策略和停止条件
- 验证标准、冲突处理和最终输出格式
- 正例、近似误匹配、权限、失败、预算和版本迁移场景

新增工作流应当只需增加目录条目、契约文档、`SKILL.md` 的渐进披露链接和测试场景，并能通过结构校验，不应要求修改校验器实现。

## Skill 的职责边界

EverySkill 负责：

- 识别任务意图和主要交付物
- 选择直接处理、单 Skill 路由或多阶段编排
- 设计任务 DAG 和拓扑
- 约束上下文、权限、预算、失败和验证
- 生成可解释的 Route Packet 和最终编排方案

EverySkill 不负责：

- 实际调用模型、工具或外部系统
- 替代宿主 Agent 的权限系统
- 提供持久化任务队列、事件总线或生产调度器
- 假设所有宿主都支持多 Agent 并发
- 在缺少能力或权限时模拟已经完成的执行

实际执行由宿主环境负责；当宿主能力不足时，编排应安全降级、请求澄清或阻塞。

## 目标形态

仓库的核心应保持为一个可移植的 Skill 包：

```text
SKILL.md                         核心路由与编排协议
references/workflow-catalog.md   工作流匹配目录
references/workflow-*.md         可替换的工作流契约
references/workflow-scenarios.json 场景与覆盖规范
references/validation-policy.json  可配置的结构校验策略
references/behavior-evaluation-*.json 离线行为评估与人工评审格式
references/workflow-composition.md 多工作流组合契约
scripts/validate_workflows.py    配置结构校验
scripts/topology_analysis.py     图结构指标与候选建议
scripts/report_coverage.py       确定性结构覆盖报告
scripts/scaffold_workflow.py     保守的工作流脚手架
tests/                           校验器回归测试
```

可视化界面、实验性客户端或其他演示工具只能作为外部辅助，不属于 Skill 的必要运行时组成部分。

## 成功标准

项目达到目标时，针对一个新问题或一组自定义 Skill，宿主 Agent 能够：

1. 说明为什么选择直接处理、单路由、串行、并行或混合拓扑。
2. 输出节点、边、依赖、负责人、上下文和汇合策略。
3. 明确列出权限、预算、风险、验证、失败和停止条件。
4. 在相似但意图不同的请求之间保持稳定的工作流选择。
5. 在缺少能力、权限或新鲜上下文时安全阻塞或降级，而不是虚构执行结果。
6. 通过结构化契约和场景测试验证新增工作流不会破坏既有路由规则。
7. 通过确定性的结构指标和覆盖报告发现图、预算和案例缺口，同时明确这些结果不能替代真实 Agent 行为证据。

最终目标不是让每个请求都拆得更复杂，而是让每个请求都获得与其依赖关系、风险和质量要求相匹配的最小充分编排。
