# Agent 协作说明

## 每次迭代必须更新现状

每次对本仓库进行一轮有意义的迭代后，必须同步更新根目录的 [STATUS.md](./STATUS.md)。这里的“一轮迭代”包括新增、删除或修改协议、工作流契约、场景、校验器、测试或仓库结构。

更新 `STATUS.md` 时至少检查并同步以下内容：

- 当前项目定位是否发生变化
- 本轮已完成的能力和文件变更
- 当前仍未完成的事项和已知限制
- 校验器和测试的最新结果
- 下一步优先级是否需要调整

不要只记录代码变更，也要记录协议行为、配置方式、权限边界、验证覆盖和非目标的变化。如果本轮没有改变项目能力，只进行了纯格式化或临时调试，可以不改动现状正文，但应确认现状仍然准确。

## 完成前检查

在结束迭代前：

1. 阅读 `TARGET.md`，确认变更仍符合项目目标。
2. 阅读并更新 `STATUS.md`，确保它描述的是当前仓库而不是旧状态。
3. 运行 `python scripts/validate_workflows.py`。
4. 运行 `python -m pytest -q`。
5. 在交付说明中报告现状文档是否已同步，以及验证结果。

## 范围边界

EverySkill 是 Markdown/JSON 驱动的编排 Skill。除非用户明确要求，否则不要将其扩展为前后端系统、持久化调度器或真实 Agent 执行平台。实际执行能力、工具调用和权限控制属于宿主 Agent 环境。
