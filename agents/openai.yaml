interface:
  display_name: "Everyskill"
  short_description: "Adaptively orchestrate agents and skills"
  default_prompt: "Use $everyskill to choose the simplest sufficient route; keep simple requests on the fast path and build a task graph only when coordination materially helps."

policy:
  allow_implicit_invocation: true
